export enum STORAGE_KEYS {
    USERS = 'users',
    CURRENT_USER = 'currentUser',
    SESSIONS = 'sessions',
    THREADS = 'threads',
    POSTS = 'posts',
    NOTIFICATIONS = 'notifications',
    THEME = 'theme',
    LAST_SEEN_PREFIX = 'lastSeen_',
    TYPING_PREFIX = 'typing_',
    WALLET_PREFIX = 'wallet_',
    TRANSACTIONS_PREFIX = 'transactions_',
    PAYOUTS_PREFIX = 'payouts_',
}
